---
label: Gallery
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: body
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: markdown
  label: Body
- name: gallery
  type: field_group_list
  fields:
  - name: before
    type: file
    config:
      maxSize: 64
    label: Before
  - name: after
    type: file
    config:
      maxSize: 64
    label: After
  config:
    min: 
    max: 
    labelField: 
  label: Gallery
pages:
- content/gallery/_index.md
